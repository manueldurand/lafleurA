{% extends 'security/base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <form class="form w-100" method="POST" action="{{ path('app_login') }}" id="kt_sign_in_form" >
        <div class="text-center mb-10">
            <h1 class="text-dark fw-bolder mb-3">Connexion</h1>
            <div class="text-gray-500 fw-semibold fs-6">{{ app_name }}</div>
        </div>

        {% if error %}
            <div class="alert alert-dismissible bg-danger d-flex flex-column flex-sm-row w-100 p-5 mb-10">
                <div class="d-flex flex-column text-light pe-0 pe-sm-10">
                    <h4 class="mb-2 text-light">Erreur</h4>
                    <span>{{ error.messageKey|trans(error.messageData, 'security') }}</span>
                </div>
            </div>
        {% endif %}

        <div class="fv-row mb-8">
            <input type="text" placeholder="Email" name="_username" autocomplete="off" class="form-control bg-transparent" />
        </div>

        <div class="fv-row mb-20">
            <input type="password" placeholder="Password" name="_password" autocomplete="off" class="form-control bg-transparent" />
        </div>

        <div class="d-flex flex-stack flex-wrap gap-3 fs-base fw-semibold mb-8">
            <div></div>
            <a href="{{ path('reset_password_request') }}" class="link-primary">Mot de passe oublié ?</a>
        </div>

        <div class="d-grid mb-10">
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <button type="submit" id="kt_sign_in_submit" class="btn btn-primary">
                <span class="indicator-label">Connexion</span>
            </button>
        </div>
    </form>
{% endblock %}
